"""Domain real/fake classifier examples for MAVIC-T.

This package provides training and scoring utilities for a binary classifier
that estimates how likely an image belongs to a target domain (``real``) vs.
``fake``. It is designed for no-reference quality evaluation of generated
images on val/test splits.
"""

