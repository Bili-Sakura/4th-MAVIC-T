# Baseline Model Configurations & Parameter Counts

> Auto-generated by instantiating each baseline model from its **default `TaskConfig`** and counting parameters.

## Summary

| Baseline | Architecture | Total Params | Trainable Params | # Models |
|----------|-------------|-------------|-----------------|----------|
| **DDBM** | ADM UNet (conditional) | 120.25M | 120.25M | 1 |
| **I2SB** | ADM UNet (conditional) | 120.25M | 120.25M | 1 |
| **DDIB** | ADM UNet (unconditional) × 2 | 240.49M | 240.49M | 2 |
| **BiBBDM** | ADM UNet (conditional, dual-output) | 120.25M | 120.25M | 1 |
| **CUT** | ResNet Generator + PatchGAN Discriminator | 14.14M | 14.14M | 2 |
| **Img2Img-Turbo** | SD-Turbo (LoRA-adapted) | ~981M | ~5M (LoRA only) | 1 |

> **Note**: Parameter counts above use the **default `TaskConfig()`** values (resolution=256, model\_channels=1 for diffusion baselines; model\_channels=3 for CUT). Task-specific configs (e.g. `sar2eo`, `rgb2ir`) may change `model_channels` and `resolution`, which affects the parameter count.

---

## 1. DDBM (Denoising Diffusion Bridge Models)

**Architecture**: ADM-style UNet wrapping `diffusers.UNet2DModel` with channel-concatenation conditioning.

| Parameter | Value |
|-----------|-------|
| **Total Parameters** | **120,246,401 (120.25M)** |
| **Trainable Parameters** | **120,246,401 (120.25M)** |

### Default Model Config

| Field | Value | Description |
|-------|-------|-------------|
| `unet_type` | `"adm"` | ADM (Ablated Diffusion Model) architecture |
| `resolution` | `256` | Input image resolution |
| `model_channels` | `1` | Number of channels the UNet operates on |
| `num_channels` | `128` | Base channel width of the UNet |
| `num_res_blocks` | `2` | Residual blocks per resolution level |
| `attention_resolutions` | `"32,16,8"` | Resolutions at which self-attention is applied |
| `dropout` | `0.0` | Dropout rate |
| `condition_mode` | `"concat"` | Source image concatenated on channel dim |
| `channel_mult` | `""` (auto) | Auto-detected as `(1,1,2,2,4,4)` for 256px |

### Scheduler Config

| Field | Value | Description |
|-------|-------|-------------|
| `pred_mode` | `"vp"` | Variance-preserving SDE |
| `sigma_max` | `1.0` | Maximum noise level |
| `sigma_min` | `0.002` | Minimum noise level |
| `sigma_data` | `0.5` | Data standard deviation |
| `beta_d` | `2.0` | Diffusion coefficient |
| `beta_min` | `0.1` | Minimum beta |
| `num_inference_steps` | `40` | Sampling steps during evaluation |

---

## 2. I2SB (Image-to-Image Schrödinger Bridge)

**Architecture**: ADM-style UNet wrapping `diffusers.UNet2DModel` with channel-concatenation conditioning. Same UNet as DDBM but with I2SB-specific scheduler.

| Parameter | Value |
|-----------|-------|
| **Total Parameters** | **120,246,401 (120.25M)** |
| **Trainable Parameters** | **120,246,401 (120.25M)** |

### Default Model Config

| Field | Value | Description |
|-------|-------|-------------|
| `unet_type` | `"adm"` | ADM architecture |
| `resolution` | `256` | Input image resolution |
| `model_channels` | `1` | Number of channels the UNet operates on |
| `num_channels` | `128` | Base channel width of the UNet |
| `num_res_blocks` | `2` | Residual blocks per resolution level |
| `attention_resolutions` | `"32,16,8"` | Resolutions at which self-attention is applied |
| `dropout` | `0.0` | Dropout rate |
| `condition_mode` | `"concat"` | Source image concatenated on channel dim |
| `channel_mult` | `""` (auto) | Auto-detected as `(1,1,2,2,4,4)` for 256px |

### Scheduler Config

| Field | Value | Description |
|-------|-------|-------------|
| `interval` | `1000` | Number of diffusion timesteps |
| `beta_max` | `0.3` | Maximum diffusion rate |
| `t0` | `1e-4` | Start time |
| `T` | `1.0` | End time |
| `ot_ode` | `False` | OT-ODE path (deterministic sampling) |
| `nfe` | `100` | Number of function evaluations during sampling |

---

## 3. DDIB (Dual Diffusion Implicit Bridges)

**Architecture**: Two independent **unconditional** ADM-style UNets (one per domain). Translation via deterministic DDIM encode→decode ODE through shared latent space.

| Parameter | Value |
|-----------|-------|
| **Per-Model Parameters** | **120,245,249 (120.25M)** |
| **Total Parameters (2 models)** | **240,490,498 (240.49M)** |
| **Trainable Parameters** | **240,490,498 (240.49M)** |

### Default Model Config

| Field | Value | Description |
|-------|-------|-------------|
| `resolution` | `256` | Input image resolution |
| `model_channels` | `1` | Number of channels per unconditional UNet |
| `num_channels` | `128` | Base channel width |
| `num_res_blocks` | `2` | Residual blocks per resolution level |
| `attention_resolutions` | `"32,16,8"` | Resolutions at which self-attention is applied |
| `dropout` | `0.0` | Dropout rate |
| `learn_sigma` | `False` | Whether to predict log-variance (doubles output channels) |
| `channel_mult` | `""` (auto) | Auto-detected as `(1,1,2,2,4,4)` for 256px |

### Diffusion / Scheduler Config

| Field | Value | Description |
|-------|-------|-------------|
| `diffusion_steps` | `1000` | Total diffusion timesteps |
| `noise_schedule` | `"linear"` | Noise schedule type |
| `predict_xstart` | `False` | Predict x₀ directly |
| `num_inference_steps` | `250` | DDIM steps for encode/decode |
| `eta` | `0.0` | DDIM eta (0 = deterministic ODE) |

---

## 4. BiBBDM (Bidirectional Brownian Bridge Diffusion Models)

**Architecture**: ADM-style UNet wrapping `diffusers.UNet2DModel` with channel-concatenation conditioning and **dual-output** (predicts both signal and noise jointly for `dlns` objective).

| Parameter | Value |
|-----------|-------|
| **Total Parameters** | **120,247,554 (120.25M)** |
| **Trainable Parameters** | **120,247,554 (120.25M)** |

### Default Model Config

| Field | Value | Description |
|-------|-------|-------------|
| `resolution` | `256` | Input image resolution |
| `model_channels` | `1` | Number of channels the UNet operates on |
| `num_channels` | `128` | Base channel width |
| `num_res_blocks` | `2` | Residual blocks per resolution level |
| `attention_resolutions` | `"32,16,8"` | Resolutions at which self-attention is applied |
| `dropout` | `0.0` | Dropout rate |
| `condition_mode` | `"concat"` | Source image concatenated on channel dim |
| `channel_mult` | `""` (auto) | Auto-detected as `(1,1,2,2,4,4)` for 256px |
| `objective` | `"dlns"` | Dual-loss noise+signal prediction → `out_channels = 2 × in_channels` |

### Brownian Bridge Config

| Field | Value | Description |
|-------|-------|-------------|
| `num_timesteps` | `1000` | Bridge timesteps |
| `mt_type` | `"linear"` | Mean trajectory type (linear / sin / log) |
| `m0` | `0.001` | Initial mean |
| `mT` | `0.999` | Terminal mean |
| `eta` | `1.0` | Fast sampling eta |
| `var_scale` | `2.0` | Variance scale |
| `loss_type` | `"l1"` | Loss function (l1 / l2) |
| `sample_step` | `100` | Sampling steps during evaluation |

---

## 5. CUT (Contrastive Unpaired Translation)

**Architecture**: ResNet-based Generator (9 residual blocks) + PatchGAN Discriminator (70×70 receptive field). Single-pass translation (no iterative denoising).

| Component | Parameters | Trainable |
|-----------|-----------|-----------|
| **Generator** (ResNet-9blocks) | **11,378,179 (11.38M)** | 11.38M |
| **Discriminator** (PatchGAN) | **2,764,737 (2.76M)** | 2.76M |
| **Total (G+D)** | **14,142,916 (14.14M)** | **14.14M** |

### Default Generator Config

| Field | Value | Description |
|-------|-------|-------------|
| `model_channels` | `3` | Input/output channels |
| `netG` | `"resnet_9blocks"` | Generator architecture (9 ResNet blocks) |
| `ngf` | `64` | Base filter count in generator |
| `normG` | `"instance"` | Normalization type |
| `no_dropout` | `True` | Dropout disabled |
| `no_antialias` | `False` | Use anti-aliased downsampling |
| `no_antialias_up` | `False` | Use anti-aliased upsampling |
| `init_type` | `"xavier"` | Weight initialization |
| `init_gain` | `0.02` | Initialization gain |

### Default Discriminator Config

| Field | Value | Description |
|-------|-------|-------------|
| `netD` | `"basic"` | PatchGAN discriminator (70×70) |
| `ndf` | `64` | Base filter count in discriminator |
| `n_layers_D` | `3` | Number of discriminator layers |
| `normD` | `"instance"` | Normalization type |

### Contrastive Loss Config

| Field | Value | Description |
|-------|-------|-------------|
| `CUT_mode` | `"CUT"` | CUT (vs. FastCUT) |
| `lambda_GAN` | `1.0` | GAN loss weight |
| `lambda_NCE` | `1.0` | Contrastive loss weight |
| `nce_idt` | `True` | Identity NCE loss |
| `nce_layers` | `"0,4,8,12,16"` | Layers for NCE feature extraction |
| `nce_T` | `0.07` | Contrastive temperature |
| `num_patches` | `256` | Number of patches for NCE |
| `netF` | `"mlp_sample"` | Feature network (PatchSampleF) |
| `netF_nc` | `256` | Feature network hidden channels |

---

## 6. Img2Img-Turbo (Pix2Pix-Turbo)

**Architecture**: LoRA-adapted **Stable Diffusion Turbo** backbone. Single-step denoising at `t=999`. Comprises a frozen text encoder (CLIP), UNet with LoRA adapters, and VAE with LoRA adapters and learned skip connections.

> **Note**: This baseline requires downloading the `stabilityai/sd-turbo` checkpoint from HuggingFace Hub. Parameter counts below are based on the published SD-Turbo architecture.

| Component | Total Parameters | Trainable (LoRA) |
|-----------|-----------------|-------------------|
| **Text Encoder** (CLIP) | ~123M | 0 (frozen) |
| **VAE** (AutoencoderKL) | ~84M | LoRA rank-4 adapters |
| **UNet** (UNet2DConditionModel) | ~860M | LoRA rank-8 adapters |
| **Skip Connections** (4 × Conv2d) | ~0.01M | ~0.01M |
| **Total Backbone** | **~981M** | **~5M** |

### Default Model Config

| Field | Value | Description |
|-------|-------|-------------|
| `pretrained_model_name_or_path` | `"stabilityai/sd-turbo"` | Base model checkpoint |
| `lora_rank_unet` | `8` | LoRA rank for UNet adapters |
| `lora_rank_vae` | `4` | LoRA rank for VAE adapters |
| `resolution` | `512` | Input image resolution (SD-Turbo native) |
| `model_channels` | `3` | SD-Turbo VAE expects 3-channel RGB |

### Training Config

| Field | Value | Description |
|-------|-------|-------------|
| `lambda_l2` | `1.0` | L2 reconstruction loss weight |
| `lambda_lpips` | `5.0` | LPIPS perceptual loss weight |
| `max_grad_norm` | `1.0` | Gradient clipping |
| `weight_decay` | `1e-2` | Weight decay |

---

## Notes

- **Channel multiplier**: When `channel_mult` is empty (default), it is auto-detected from the resolution. For 256px and 512px: `(1,1,2,2,4,4)`. For 128px: `(1,1,2,3,4)`. For 64px and 32px: `(1,2,3,4)`. For 1024px (used in `rgb2ir`, `sar2ir`, `sar2rgb` tasks): falls back to `(1,2,3,4)`.
- **Conditioning**: DDBM, I2SB, and BiBBDM concatenate the source image along the channel dimension (`condition_mode="concat"`). DDIB is unconditional (no source conditioning). CUT and Img2Img-Turbo use their own conditioning mechanisms.
- **DDIB trains 2 models**: One for the source domain and one for the target domain. Translation is performed via DDIM reverse encoding → DDIM forward decoding through a shared latent space.
- **BiBBDM dual output**: With the default `"dlns"` objective, the UNet outputs `2 × in_channels` (predicting both `(b-a)` difference and noise simultaneously).
- **Img2Img-Turbo** is noted as lower priority for the MAVIC-T task compared to other baselines. Its code is retained for reference.
